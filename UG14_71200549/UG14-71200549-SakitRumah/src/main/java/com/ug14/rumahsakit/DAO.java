package com.ug14.rumahsakit;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;
import java.sql.Statement;

public class DAO {
    /*
    public void inputPasien(Pasien p) throws SQLException {
        String url = "jdbc:sqlite:D:/[source]/";
        Connection conn = DriverManager.getConnection(url, "root", "");
        Statement statement = ((Connection) conn).createStatement();
        String query = "INSERT INTO pasien values ('"+pasien.getRm()+"','"+pasien.getNama()+"','"+pasien.getUsia()+"','"+pasien.getAlamat()+"')";
        statement.executeUpdate(query);
        conn.close();
    }
    */
}
